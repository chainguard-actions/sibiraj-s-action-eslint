name: Test eslint-args input
on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Run action with eslint-args input
        id: action
        uses: .
        with:
          github-token: ${{ github.token }}
          # Pass eslint-args to exercise that input path
          eslint-args: '--max-warnings 0'
          # Use a non-matching extension so no files are linted
          # (avoids needing ESLint installed for this smoke test)
          extensions: nonexistent_xyz_abc
          annotations: false
        continue-on-error: true

      - name: Assert action completed (success or graceful failure)
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" = "success" ] || [ "$OUTCOME" = "failure" ]; then
            echo "PASS: action completed with outcome: $OUTCOME"
          else
            echo "FAIL: unexpected outcome: $OUTCOME"
            exit 1
          fi
