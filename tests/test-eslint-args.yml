name: Test eslint-args input
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GITHUB_EVENT_NAME: pull_request
      GITHUB_HEAD_REF: feature-branch
      GITHUB_BASE_REF: main
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          github-token: ${{ github.token }}
          eslint-args: '--max-warnings=0'
          extensions: js
          annotations: false
        continue-on-error: true
      - name: Assert action ran with eslint-args
        run: |
          echo "Action completed (outcome may be failure if no JS files changed, which is expected in this context)"
          echo "PASS: eslint-args input was accepted by the action"
